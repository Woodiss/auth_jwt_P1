<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Site de Spectacles{% endblock %}</title>
    <link
    rel="stylesheet"
    href="https://cdn.jsdelivr.net/npm/@picocss/pico@2/css/pico.min.css"
    >
</head>
<body>

    <nav style="padding: 1rem 2rem;">
        <ul>
            <li><strong><a href="{{ basePath }}/home" class="contrast">SpecTax</a></strong></li>
        </ul>
        <ul>
            <li><a href="{{ basePath }}/spectacles" class="contrast">Spectacles</a></li>
            <li><a href="{{ basePath }}/home" class="contrast">Accueil</a></li>
            <details class="dropdown">
                <summary>
                    Compte
                </summary>
                <ul dir="rtl">
                    {% if user %}
                        <li><a href="{{ basePath }}/profile" class="contrast">Mon Profil / Réservation</a></li>
                        <li><a href="{{ basePath }}/logout" class="contrast">Déconnexion</a></li>
                    {% else %}
                        <li><a href="{{ basePath }}/login" class="contrast">Connexion</a></li>
                        <li><a href="{{ basePath }}/register" class="contrast">Inscription</a></li>
                    {% endif %}
                </ul>
            </details>
        </ul>
    </nav>
    <main class="container">
        {# C'est ici que le contenu des autres pages sera injecté #}
        {% block content %}{% endblock %}
    </main>

</body>
</html>
