<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Mon Site de Spectacles{% endblock %}</title>
    {# Vous pouvez ajouter vos liens CSS ici #}
    <style>
        body { font-family: sans-serif; margin: 0; background-color: #f4f4f4; }
        nav { background: #333; color: white; padding: 1rem; }
        nav a { color: white; padding: 0.5rem; text-decoration: none; }
        nav a:hover { background: #555; }
        .container { max-width: 1000px; margin: 2rem auto; padding: 1rem; background: white; }
    </style>
</head>
<body>

    {# Le menu de navigation (contrainte de l'exercice) #}
    <nav>
        <a href="home">Accueil</a>
        <a href="spectacles">Spectacles</a>
        
        {# Affiche "Profil" ou "Login" selon la connexion #}
        {% if user %}
            <a href="profil">Mon Profil</a>
            <a href="logout">Déconnexion</a>
        {% else %}
            <a href="login">Connexion</a>
        {% endif %}
    </nav>

    <main class="container">
        {# C'est ici que le contenu des autres pages sera injecté #}
        {% block content %}{% endblock %}
    </main>

</body>
</html>
