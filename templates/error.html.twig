
<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Erreur {{ code }}</title>
  <style>
    body { font-family: sans-serif; text-align: center; padding: 2rem; }
    h1 { font-size: 3rem; color: #b00; }
  </style>
</head>
<body>
  <h1>Erreur {{ code }}</h1>
  <p>{{ message }}</p>
  <a href="{{ basePath }}/home">← Retour à l'accueil</a>
</body>
</html>
